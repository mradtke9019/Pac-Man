#version 330

in vec4 color;
out vec4 FragColor;
uniform float time;
uniform mat4 MVP;
uniform mat4 Identity;
uniform float rand;


void main()                                                             
{
	
	FragColor = color;
	FragColor = vec4(rand * color.r, 1 - rand *  color.g, rand * rand * color.b, 1.0f);
	//FragColor = vec4(1.0,0.0,0.0,1.0);
}